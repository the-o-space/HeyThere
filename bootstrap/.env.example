# Bootstrap Node Configuration
# Copy this to .env and fill in your values

# Port for WebSocket connections (default: 4001)
PORT=4001

# Your public IP address (required for others to connect)
# Leave as 0.0.0.0 for local testing only
ANNOUNCE_IP=0.0.0.0
